{
  "name": "ali-ahnach-portfolio",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "",
    "dev": "pnpm --filter @portfolio/web run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:web": "pnpm --filter @portfolio/web run build",
    "preview:web": "pnpm --filter @portfolio/web run serve",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
